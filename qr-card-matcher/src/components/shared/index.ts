/**
 * Shared Components
 * Components that work on both desktop and mobile platforms
 */

// Re-export shared components from their current locations
// These will be moved here in future refactoring

export { default as LoginPage } from '../LoginPage';
export { QRScanner } from '../QRScanner';
export { default as QRGenerator } from '../QRGenerator';
export { default as WeekDisplay } from '../WeekDisplay';
export { default as ReminderModal } from '../ReminderModal';
